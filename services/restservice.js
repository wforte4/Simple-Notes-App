

// Base Url for the rest api -- Every other directory can be accessed baseURL + "/%s";
export const baseConfig = {
  baseURL: "http://localhost:3600",
  backendImages: 'http://localhost:3600/ftp/',
  projectURL: "http://localhost:3600/projects/"
};
